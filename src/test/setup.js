/**
 * Vitest global test setup — extends matchers with @testing-library/jest-dom
 * so all test files have access to DOM-specific assertions like toBeInTheDocument().
 */
import "@testing-library/jest-dom";
